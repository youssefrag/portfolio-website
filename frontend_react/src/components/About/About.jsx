import React from "react";

import "./About.scss";

const About = () => {
  return (
    <div className="about-section container">
      <div className="subheading">
        Full Stack web developer trying to break into the industry, recent
        bootcamp grad from Lighthouse Labs. Skills include but not limited to
        Javascript, SQL, React, HTML, CSS, Material UI, Postgres and Solidity.
      </div>
    </div>
  );
};

export default About;
